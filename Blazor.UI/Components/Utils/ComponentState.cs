namespace Blazor.UI
{
    public enum ComponentState
    {
        New,
        Loading,
        Loaded,
        InError
    }
    public enum ModalResultType { 
        NoSet, 
        OK, 
        Cancel, 
        Exit 
    }
}
